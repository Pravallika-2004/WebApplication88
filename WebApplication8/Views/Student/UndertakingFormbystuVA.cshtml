@model WebApplication8.Models.UndertakingbyparVA
@{
    Layout = null;
    ViewBag.Title = "Parent Undertaking Form - University Admin Dashboard";
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title</title>
    <link href="https://fonts.googleapis.com/css2?family=Times+New+Roman:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Times New Roman', Times, serif;
            margin: 10mm;
            padding: 0;
            line-height: 1.2;
            font-size: 12pt;
            width: 210mm;
            height: 297mm;
            box-sizing: border-box;
        }

        .form-container {
            width: 100%;
            padding: 5mm;
            box-sizing: border-box;
        }

        .annexure {
            text-align: right;
            font-size: 10pt;
            font-weight: 600;
            color: #000;
            margin-bottom: 3mm;
        }

        h2 {
            text-align: center;
            text-decoration: underline;
            color: #000;
            font-weight: 700;
            font-size: 14pt;
            margin: 0 0 5mm;
        }

        p {
            margin: 3mm 0;
            color: #000;
            font-size: 10pt;
        }

        .indent {
            text-indent: 10mm;
        }

        .field {
            display: inline;
            vertical-align: bottom;
            min-width: 50px;
            max-width: 200px;
            margin: 0 2mm;
            background: #f5f5f5;
            padding: 1mm 2mm;
            border-radius: 2px;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        .field-short {
            display: inline;
            vertical-align: bottom;
            min-width: 30px;
            max-width: 60px;
            margin: 0 2mm;
            background: #f5f5f5;
            padding: 1mm 2mm;
            border-radius: 2px;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        .field-long {
            display: inline;
            vertical-align: bottom;
            min-width: 100px;
            max-width: 300px;
            margin: 0 2mm;
            background: #f5f5f5;
            padding: 1mm 2mm;
            border-radius: 2px;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        .field-multiline {
            display: block;
            min-width: 100%;
            max-width: 100%;
            margin: 2mm 0;
            background: #f5f5f5;
            padding: 1mm 2mm;
            border-radius: 2px;
            white-space: normal;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        .prefilled-text {
            font-size: 10pt;
            color: #000;
            display: inline;
        }

        .parent-indicator {
            color: #000;
            font-weight: 600;
            display: block;
            margin: 2mm 0;
            font-size: 10pt;
        }

        .signature-section {
            margin-top: 5mm;
            padding: 3mm;
            text-align: right;
        }

            .signature-section label {
                display: inline-block;
                width: 80px;
                font-weight: 500;
                color: #000;
                margin-right: 5mm;
            }

        @@media print {
            body {
                margin: 0;
                width: 210mm;
                height: 297mm;
            }

            .form-container {
                padding: 5mm;
            }

            .field, .field-short, .field-long, .field-multiline {
                background: #f5f5f5;
            }

            @@page {
                size: A4;
                margin: 10mm;
            }
        }
    </style>
</head>
<body>
    <div class="form-container">
        <div class="annexure">Annexure-V (A)</div>
        <h2>UNDERTAKING OF THE PARENT / GUARDIAN</h2>

        <p class="indent">
            I, Mr./Mrs. <span class="field-long"><span class="prefilled-text">@Model.ParentName</span></span>,
            aged <span class="field-short"><span class="prefilled-text">@Model.ParentAge</span></span> years,
            address <span class="field-multiline"><span class="prefilled-text">@Model.ParentAddressLine1</span></span>
            <span class="field-multiline"><span class="prefilled-text">@Model.ParentAddressLine2</span></span>
            <span class="parent-indicator">✓ Parent</span>
            parent/guardian of <span class="field-long"><span class="prefilled-text">@Model.StudentName, (@Model.EnrollmentNo)</span></span>
            (Full name of the student with Admission/Enrollment Number) studying
            <span class="field-long"><span class="prefilled-text">@Model.StudentProgram</span></span>
            program in IFHE, Hyderabad and staying in Hostel, hereby authorize the University to allow my ward
            to leave IFHE Campus with proper and prior intimation / permission of the authorities while studying at the University.
        </p>

        <p class="indent">
            I undertake full responsibility for all his / her actions whilst he/she goes out of the Hostel/University campus.
        </p>

        <p class="indent">
            Further, I/We authorize, Mr./Mrs. <span class="field-long"><span class="prefilled-text">@Model.LocalGuardianName</span></span>,
            aged <span class="field-short"><span class="prefilled-text">@Model.LocalGuardianAge</span></span> years,
            Address <span class="field-multiline"><span class="prefilled-text">@Model.LocalGuardianAddressLine1</span></span>
            <span class="field-multiline"><span class="prefilled-text">@Model.LocalGuardianAddressLine2</span></span>
            as the LOCAL GUARDIAN of my ward. My ward may avail leave and go to the above mentioned LOCAL GUARDIAN’S place
            as per the approved leave regulations of the University.
        </p>

        <div class="signature-section">
            <p>Signature of the Parent/Guardian</p>
            <p><label>Date:</label><span class="field"></span></p>
            <p><label>Place:</label><span class="field"></span></p>
        </div>
    </div>
</body>
</html>